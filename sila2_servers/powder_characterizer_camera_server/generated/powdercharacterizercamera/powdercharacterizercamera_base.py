# Generated by sila2.code_generator; sila2.__version__: 0.14.0
from __future__ import annotations

from abc import ABC, abstractmethod
from typing import TYPE_CHECKING

from sila2.server import FeatureImplementationBase, MetadataDict

from .powdercharacterizercamera_types import Capture_Responses, CaptureRepose_Responses

if TYPE_CHECKING:

    from ...server import Server


class PowderCharacterizerCameraBase(FeatureImplementationBase, ABC):
    parent_server: Server

    def __init__(self, parent_server: Server):
        """
        Capture unchanged image bytes from the Powder Characterizer camera.
        """
        super().__init__(parent_server=parent_server)

    @abstractmethod
    def get_Status(self, *, metadata: MetadataDict) -> str:
        """
        Camera server state.

        :param metadata: The SiLA Client Metadata attached to the call
        :return: Camera server state.
        """

    @abstractmethod
    def Capture(
        self, Rotation: int, AutofocusMode: str, LensPosition: float, *, metadata: MetadataDict
    ) -> Capture_Responses:
        """
        Capture a preview using the requested focus and rotation values.


        :param Rotation: Rotation in degrees.

        :param AutofocusMode: rpicam autofocus mode.

        :param LensPosition: Manual lens position; ignored unless mode is manual.

        :param metadata: The SiLA Client Metadata attached to the call

        :return:

            - ImageData: Original JPEG bytes.

            - MediaType: HTTP media type.

            - Filename: Suggested filename.

            - Width: Pixel width.

            - Height: Pixel height.


        """

    @abstractmethod
    def CaptureRepose(self, *, metadata: MetadataDict) -> CaptureRepose_Responses:
        """
        Capture a PNG with the existing angle-of-repose camera settings.


        :param metadata: The SiLA Client Metadata attached to the call

        :return:

            - ImageData: Original PNG bytes.

            - MediaType: HTTP media type.

            - Filename: Suggested filename.

            - Width: Pixel width.

            - Height: Pixel height.


        """
